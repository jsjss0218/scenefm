import React from "react";
import ReactDOM from "react-dom/client";
import SceneFM from "./SceneFM.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SceneFM />
  </React.StrictMode>
);
